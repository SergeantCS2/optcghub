# generated by the pipeline — never commit, always rebuild (landmines 6, 22)
www/bundle/
www/app.js
www/index.html
android/
node_modules/
__pycache__/
*.pyc
# pipeline scratch
tcgcsv_cache/
img_cache/
*_payload.json
catalog/catalog.sqlite
catalog/manifest.json
catalog/catalog.json.gz
# catalog/hashes.json, catalog/prices_daily.json AND catalog/star_template.json
# ARE committed on purpose
# (landmines 46, 66): derived data that is expensive or irreplaceable lives
# outside the disposable catalogue.: expensive to derive,
# keyed on a stable productId, and it saves 14 minutes of network per build.
# NEVER commit the Play upload key (RUNBOOK-play A.3, landmine 23)
*upload*.jks
*upload*.b64
